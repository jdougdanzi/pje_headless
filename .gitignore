build/
bin/
/pjeheadless
certificate/**.pfx
pjeoffice_headless.egg-info

# Segredos — nunca versionar (certificados, chaves, env)
*.pfx
*.p12
*.pem
*.key
*.jks
cj.txt
.env
.env.*

# Artefatos de debug e planejamento interno (nao publicar)
test.sh
docs/

# Runner de validacao local (contem caminhos do host do token; nao publicar)
/run-local-login.sh
.DS_Store
